# Additional requirements to run the REAL trained models
# (LogReg + BERTić + ensemble). Install alongside requirements.txt:
#     pip install -r requirements.txt -r requirements-ml.txt
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
torch>=2.0
transformers>=4.36
